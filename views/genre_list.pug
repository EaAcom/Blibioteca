extends layout

block content
  div.title
    h1= "Lista de generos" 
  main.body
    div.body__catalogue
      ul
        each val in list_genres
          li 
            a(href=val.url)
              div.marco
                | #{val.name}

        else
          li There are no genre.
